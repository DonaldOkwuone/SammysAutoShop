@model SammysAutoShop.Models.ServiceType
@{
    ViewData["Title"] = "Details";
}

<h2>Details</h2>

<div class="form-border">
    <div class="form-group">
        <div class="row">
            <div class="col-sm-10">
               @Html.DisplayNameFor(m => m.Name) <input type="text" disabled asp-for="@Model.Name" />
            </div>
        
        </div>
        
    </div>
    <div class="form-group">
        <div class="row">
            <div class="col-sm-3">
                <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-sm btn-primary">Edit</a>
                <a asp-action="Index"  class="btn btn-sm btn-success">Back to list</a>
            </div>
        </div>
    </div>

</div>
